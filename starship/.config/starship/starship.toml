# Tokyo Night themed Starship configuration
# Matches your nvim Tokyo Night theme

format = """
[](#9d7cd8)\
$os\
$username\
[](bg:#7aa2f7 fg:#9d7cd8)\
$directory\
[](fg:#7aa2f7 bg:#394b70)\
$git_branch\
$git_status\
[](fg:#394b70 bg:#212234)\
$nodejs\
$rust\
$golang\
$php\
$python\
$java\
$c\
$elixir\
$elm\
$gradle\
$haskell\
$nim\
$ruby\
$scala\
[](fg:#212234)\
$line_break$character"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable it
[username]
show_always = true
style_user = "bg:#9d7cd8"
style_root = "bg:#9d7cd8"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#9d7cd8"
disabled = true # Disabled by default

[directory]
style = "bg:#7aa2f7"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = "…/"

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#06969A"
format = '[ $symbol $context ]($style) $path'

[elixir]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#394b70"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#394b70"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[python]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[php]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[ruby]
symbol = " "
style = "bg:#212234"
format = '[ $symbol ($version) ]($style)'

[character]
success_symbol = '[➜](bold #7dcfff)'
error_symbol = '[➜](bold #f7768e)'
